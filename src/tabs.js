const tabs = () => {
  const container = document.getElementById("container");
  const tagline = document.createElement("div");
  tagline.textContent = "We make the best dumplings in town, guaranteed!";
  container.appendChild(tagline);
}

export default tabs;
